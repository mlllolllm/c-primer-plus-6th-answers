所有对象，但私有对象无法使用常规方法访问

构造函数，析构函数，赋值函数

//返回值为空，当使用=时会出现无返回值的情况
不可连锁赋值
将调用构造函数重新创建临时对象并赋值，若类定义中有动态分配数组将会报错

先调用基类的构造函数，若无构造函数则会调用默认构造函数，然后调用派生类构造函数
调用析构函数时将先调用派生类析构函数再调用派生类析构函数

//可以不需要，将调用默认构造函数和析构函数
需要？

如果使用关键字virtual，将调用派生类，否则在使用基类指针或引用指向派生类的情况下将调用基类

需要？

可以。
//若有定义赋值运算则可以，否则不可以。
只有使用强制类型转换将派生类转换为基类才可以


//定义赋值运算符则可以，否则不可以。可以
可以。定义转换函数或者赋值运算符时才可以，否则不可以。

多态性

因为派生类对象包含基类对象的全部参数

因为不需要创建额外的临时对象来存储数据，不用多调用构造函数和析构函数

Corporation
PublicCorporation


horse不能直接调用kitchen的私有对象
函数重定义结构不同
